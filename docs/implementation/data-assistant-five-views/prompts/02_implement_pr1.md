# 发给 Cursor 的第二轮：批准后实现 PR-1

前提：你已经检查并批准 PR1_PLAN.md。此指令不是对其他 PR 的授权。
使用 Agent 执行；把 03/04 原图作为图片附件提供，并手动 @ 规则。

```text
@semovix-data-assistant
@docs/implementation/data-assistant-five-views/PR1_PLAN.md
@docs/implementation/data-assistant-five-views/inputs/01_code-implementation.md
@docs/implementation/data-assistant-five-views/inputs/02_five-view-checklist.md

我确认 PR1_PLAN.md 的 PR-1 范围。现在开始实际修改代码、运行测试和验证页面，不要只再次输出实施方案。
只执行 PR-1：结果合同与共用视图。不自动进入 PR-2 或 PR-3。

开工：重新检查工作区是否有变化。以确认的起点安全创建或使用本 PR 工作分支，记录 PR1_START_SHA。
推荐新分支 feat/data-assistant-results-pr1；若已存在批准的工作分支就沿用，不覆盖、不强制重建。
不回退到 31f9c6c；不擅自带入未合并且无关的改动；发现源文件冲突只暂停受影响修改。

实施：
1. 在已有 AskRunResult / AskResultSnapshot 内补最小兼容的标量/表格、数值/单位/精度、实际范围和引用。
   保留旧结果可读。新增数值来源明确后，旧字符串只作兼容显示，不生成两份可独立变化的业务值。
2. 扩展现有 AskResultContent，不创建 ChatResult 与 PanelResult 两套数据源。
   对话能显示紧凑结果；右侧按需展示完整结果，同一区域图表/数据切换。视图变化不调用 runAskPlan。
   标量组件的支持不代表本 PR 已接通正式指标查询。
3. 在原右侧宿主整理计划、结果和已有依据，不只是滚到长页面一段；查看结果不默认展开全部计划与权限卡。
   返回依据前记录的结果与 Tab；来源缺少就说明，不编造历史版本、文档或 SQL。
4. 用户打开同一份完整计划时，主要执行操作放右侧；对话保留摘要/状态。
   右侧关闭时按原流程保留内嵌确认；重用原回调、计划绑定和门禁。
5. 按图片纠偏文案：人口口径完整，类型只显示“本次计算”，不默认复制两份完整四列表，不补其他街镇。
6. 缺可信数值时保持旧表/文本，不从“14.2 张/千人”或“41.2 万人”推导业务载荷。
   原 Mock 14.2/16.5、32.0/36.5 与设计样例 15.0/20.0、22.5/25.0 隔离。
   缺行级人口和床位不能倒推四列表。设计验证使用隔离样例、同一组组件，清楚标为演示。
7. 保留权限/版本/关系、幂等、失效计划、迟到响应与 HTTP/Mock/Disconnected 边界。
   不删除历史查看限制以伪装 PR-3 已完成；HTTP 失败不 fallback 到 Mock。

测试与证据：
先记录改动前相关基线测试；小步修改后跑相关测试，最终按当前 package.json/锁文件跑既有类型检查、测试、多模式构建和 Smoke。
不为了绿灯删掉安全断言、清空用户历史、替换原 Fixture 或随意改验收预期。
增加/复用：旧结果兼容、数值一致、视图切换不运行、0/空/不可算区分、重复提交和失效计划拒绝、迟到结果不抢面板。
测试无法运行就写真实原因；没有同环境基线证据，不把失败归为“原来就有”。

交付：
更新 PR1_PLAN.md 的执行记录或输出 PR1_REPORT.md，包含起点 SHA、变更文件、实际完成路径、演示路径、服务依赖、执行命令/结果/未运行原因。
从实际运行页面导出 03/04 两张独立截图，写明所处模式；紧凑呈现和 Tab 切换用交互检查证明。
截图建议保存到 docs/implementation/data-assistant-five-views/evidence/pr1/；文件不存在不得报告成功。
其余 01/02/05 留给后续 PR，不为凑五张编造实现。

提交权限：本轮只允许本地开发和测试，不自动 commit/push/merge，不自动开 PR，不改冻结标签。
完成本 PR 后停下；结果合同/视图的约定范围通过即结束，不加报告、分享、看板、通用引擎或指标入口。
```
